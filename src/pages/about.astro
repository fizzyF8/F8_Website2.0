---
import Layout from "../layouts/Layout.astro";
import VerticalNavigation from "../components/VerticalNavigation.astro";
import vinayakImage from "../assets/leadership/vinayak.jpg";
import hamidImage from "../assets/leadership/hamid.jpg";
import premImage from "../assets/leadership/prem_gupta.png";
import srinivasImage from "../assets/leadership/srinivas_shankar.jpeg";
---

<Layout title="Frog8 - About Us | Where Fintech Meets Infrastructure">
  <VerticalNavigation />

  <section class="section gradient-bg pt-32 relative overflow-hidden hero-section">
    <!-- Advanced Background Effects -->
    <div class="absolute inset-0">
      <!-- Animated Grid -->
      <div class="absolute inset-0 opacity-5">
        <div class="grid-pattern"></div>
      </div>
      
      <!-- Floating Orbs -->
      <div class="absolute top-20 left-10 w-72 h-72 bg-gradient-to-r from-green/20 to-primary/20 rounded-full blur-3xl animate-float-slow"></div>
      <div class="absolute bottom-20 right-10 w-96 h-96 bg-gradient-to-l from-primary/15 to-green/15 rounded-full blur-3xl animate-float-reverse"></div>
      <div class="absolute top-1/2 left-1/4 w-48 h-48 bg-green/10 rounded-full blur-2xl animate-drift"></div>
      
      <!-- Particle Effect -->
      <div class="particles">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
      </div>
    </div>
    
    <div class="container text-center relative z-10">
      <h1 class="hero-title mb-6 relative">
        About <span class="hero-highlight">Frog8</span>
      </h1>
      <p class="hero-subtitle text-xl max-w-4xl mx-auto">
        Deep expertise across transit, payments, and platform scaling. Building the infrastructure for India's digital future.
      </p>
    </div>
  </section>

  <div class="section-divider"></div>

  <!-- Our Vision -->
  <section class="section">
    <div class="container">
      <div class="vision-section">
        <h2 class="vision-title text-center mb-8">
          Our <span class="text-green">Vision</span>
        </h2>
        <div class="vision-content">
          <p class="vision-intro">
            India is experiencing a once-in-a-generation infrastructure expansion. A $100+ billion metro rollout spanning 25 cities and 1,800 km by 2030 is creating the nation's most valuable consumer cohort—100M+ high-value, young, educated, and highly engaged commuters.
          </p>
          <p class="vision-problem">
            Yet this cohort remains "unowned." When commuters use anonymous cash, paper tickets, or zero-KYC cards, they are invisible to the financial ecosystem. Banks lose the relationship. Valuable data leaks out daily. A multi-billion-dollar platform opportunity remains untapped.
          </p>
          <p class="vision-solution">
            Frog8 exists to solve this problem. We provide the platform that de-anonymizes the commuter at the point of transit—unlocking the entire ecosystem through instant, full-KYC digital identity.
          </p>
        </div>
      </div>
    </div>
  </section>

  <div class="section-divider"></div>

  <!-- Our Approach -->
  <section class="section bg-black/20">
    <div class="container">
      <h2 class="text-center mb-12 md:mb-16">
        Our <span class="text-green">Approach</span>
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="glass approach-card">
          <div class="approach-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <rect x="3" y="3" width="18" height="18" rx="2" />
              <path d="M3 9h18M9 21V9" />
            </svg>
          </div>
          <h3>Platform-First</h3>
          <p>We build modular, scalable platforms—not point solutions. Our transit-proven technology extends seamlessly to retail, banking, and smart city applications.</p>
        </div>
        <div class="glass approach-card">
          <div class="approach-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M9 12l2 2 4-4" />
              <path d="M21 12c0 4.97-4.03 9-9 9s-9-4.03-9-9 4.03-9 9-9 9 4.03 9 9z" />
            </svg>
          </div>
          <h3>Innovation Through Deployment</h3>
          <p>We don't theorize—we deploy. Our technology is proven at scale with Bengaluru Namma Metro, processing 100K+ daily transactions.</p>
        </div>
        <div class="glass approach-card">
          <div class="approach-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M12 2l7 4v6c0 5-3 9-7 10-4-1-7-5-7-10V6z" />
              <path d="M9 12l2 2 4-4" />
            </svg>
          </div>
          <h3>Deep Expertise</h3>
          <p>Our team brings 100+ combined years of experience in banking infrastructure, transit systems, payment platforms, and regulatory compliance.</p>
        </div>
      </div>
    </div>
  </section>

  <div class="section-divider"></div>

  <!-- Leadership Team -->
  <section class="section bg-glass-light">
    <div class="container">
      <h2 class="text-center mb-4">
        Leadership <span class="text-green">Team</span>
      </h2>
      <p class="text-center mb-12 text-lg text-white/80 max-w-3xl mx-auto">
        Proven leaders with deep, specific expertise across transit, payments, and platform scaling
      </p>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div class="glass leader-card">
          <div class="leader-image-wrapper">
            <img 
              src={vinayakImage.src} 
              alt="Vinayak Prasad, Founder" 
              class="leader-image"
              loading="lazy"
            />
          </div>
          <h3 class="leader-name">Vinayak Prasad</h3>
          <p class="leader-role">Founder</p>
          <p class="leader-bio">
            ~30 years in retail banking, fintech, and self-service automation. Leadership roles at Capital One, Verifone, and Meta. Deep expertise in payment systems and platform scaling.
          </p>
        </div>
        <div class="glass leader-card">
          <div class="leader-image-wrapper">
            <img 
              src={hamidImage.src} 
              alt="Mohammed Hamid Ansari, Co-founder" 
              class="leader-image"
              loading="lazy"
            />
          </div>
          <h3 class="leader-name">Mohammed Hamid Ansari</h3>
          <p class="leader-role">Co-founder</p>
          <p class="leader-bio">
            20+ years in banking infrastructure and platform delivery. Led grid-wide deployments with Forbes Technosys for major banks including SBI and DBS. Expert in enterprise-scale implementation.
          </p>
        </div>
        <div class="glass leader-card">
          <div class="leader-image-wrapper">
            <img 
              src={srinivasImage.src} 
              alt="Srinivas S, Product Design" 
              class="leader-image"
              loading="lazy"
            />
          </div>
          <h3 class="leader-name">Srinivas S</h3>
          <p class="leader-role">Product Design</p>
          <p class="leader-bio">
            40+ years in product design for transit, urban mobility, and smart-pay solutions. Deep experience in BRTS, metro systems, and AFCS (Automated Fare Collection Systems).
          </p>
        </div>
        <div class="glass leader-card">
          <div class="leader-image-wrapper">
            <img 
              src={premImage.src} 
              alt="Prem Gupta, Platform Architecture" 
              class="leader-image"
              loading="lazy"
            />
          </div>
          <h3 class="leader-name">Prem Gupta</h3>
          <p class="leader-role">Platform Architecture</p>
          <p class="leader-bio">
            20+ years in regulated fintech platforms. Expertise in payments, loyalty, issuance, acquiring, and system architecture. Ensures regulatory compliance and security.
          </p>
        </div>
      </div>
    </div>
  </section>

  <div class="section-divider"></div>

  <!-- The Opportunity -->
  <section class="section bg-black/20">
    <div class="container">
      <h2 class="text-center mb-12 md:mb-16">
        The <span class="text-green">Opportunity</span>
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
        <div class="glass opportunity-card">
          <div class="opportunity-value">$100B</div>
          <div class="opportunity-label">Metro Market by 2030</div>
        </div>
        <div class="glass opportunity-card">
          <div class="opportunity-value">100M</div>
          <div class="opportunity-label">High-Value Commuters</div>
        </div>
        <div class="glass opportunity-card">
          <div class="opportunity-value">30%+</div>
          <div class="opportunity-label">Daily Active Users</div>
        </div>
      </div>
      <p class="opportunity-tagline text-center text-xl max-w-3xl mx-auto">
        The platform that de-anonymizes this cohort doesn't just win a customer—it unlocks the entire ecosystem.
      </p>
    </div>
  </section>

  <div class="section-divider"></div>

  <!-- Join Our Journey -->
  <section class="section bg-glass-light">
    <div class="container">
      <div class="cta-section glass">
        <h2 class="cta-title">Join Our Journey</h2>
        <p class="cta-description">
          Whether you're a transit authority, bank, investor, or partner, we're building the infrastructure for India's digital future. Let's talk.
        </p>
        <div class="cta-buttons">
          <a href="/contact" class="btn btn-primary">Get In Touch</a>
          <a href="/deployments" class="btn btn-secondary">See Our Deployments</a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Advanced Hero Styles */
  .hero-section {
    /* min-height: 100vh; */
    display: flex;
    align-items: center;
  }

  /* Animated Grid Pattern */
  .grid-pattern {
    background-image: 
      linear-gradient(rgba(167, 210, 33, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(167, 210, 33, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    width: 100%;
    height: 100%;
    animation: gridMove 20s linear infinite;
  }

  @keyframes gridMove {
    0% { transform: translate(0, 0); }
    100% { transform: translate(50px, 50px); }
  }

  /* Advanced Float Animations */
  @keyframes animate-float-slow {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    33% { transform: translateY(-20px) rotate(120deg); }
    66% { transform: translateY(10px) rotate(240deg); }
  }

  @keyframes animate-float-reverse {
    0%, 100% { transform: translateY(0px) rotate(0deg) scale(1); }
    50% { transform: translateY(-30px) rotate(180deg) scale(1.1); }
  }

  @keyframes animate-drift {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    25% { transform: translate(30px, -20px) rotate(90deg); }
    50% { transform: translate(-20px, -40px) rotate(180deg); }
    75% { transform: translate(-40px, 20px) rotate(270deg); }
  }

  .animate-float-slow { animation: animate-float-slow 8s ease-in-out infinite; }
  .animate-float-reverse { animation: animate-float-reverse 12s ease-in-out infinite; }
  .animate-drift { animation: animate-drift 15s ease-in-out infinite; }

  /* Particle System */
  .particles {
    position: absolute;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  .particle {
    position: absolute;
    width: 4px;
    height: 4px;
    background: rgba(167, 210, 33, 0.6);
    border-radius: 50%;
    animation: particleFloat 8s linear infinite;
  }

  .particle:nth-child(1) { left: 10%; animation-delay: 0s; animation-duration: 8s; }
  .particle:nth-child(2) { left: 30%; animation-delay: 2s; animation-duration: 12s; }
  .particle:nth-child(3) { left: 50%; animation-delay: 4s; animation-duration: 10s; }
  .particle:nth-child(4) { left: 70%; animation-delay: 6s; animation-duration: 14s; }
  .particle:nth-child(5) { left: 90%; animation-delay: 8s; animation-duration: 9s; }

  @keyframes particleFloat {
    0% { 
      transform: translateY(100vh) scale(0);
      opacity: 0;
    }
    10% {
      opacity: 1;
      transform: translateY(90vh) scale(1);
    }
    90% {
      opacity: 1;
      transform: translateY(10vh) scale(1);
    }
    100% { 
      transform: translateY(-10vh) scale(0);
      opacity: 0;
    }
  }

  /* Hero Typography */
  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 2rem;
  }

  .hero-text-line {
    display: inline-block;
    opacity: 1;
    transform: translateY(0);
  }

  .hero-highlight {
    display: inline-block;
    background: linear-gradient(135deg, #a7d221, #bdfc52);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: #a7d221; /* fallback color */
    position: relative;
    opacity: 1;
    transform: translateY(0);
  }

  /* Fallback for browsers that don't support background-clip */
  @supports not (-webkit-background-clip: text) {
    .hero-highlight {
      -webkit-text-fill-color: initial;
      color: #a7d221;
    }
  }

  .hero-highlight::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 3px;
    background: linear-gradient(90deg, #a7d221, #bdfc52);
    animation: underlineExpand 1s ease-out 1s forwards;
  }

  @keyframes textReveal {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes underlineExpand {
    to { width: 100%; }
  }

  .hero-subtitle {
    opacity: 1;
    transform: translateY(0);
  }

  /* Section Divider */ 
  @media (max-width: 900px) {
    .section-divider {
      height: 1.1rem;
      margin-bottom: 2rem;
    }
  }
  /* Green dot bullets */
  .dot {
    width: 0.82rem;
    height: 0.82rem;
    background: var(--primary);
    border-radius: 50%;
    display: inline-block;
    box-shadow: 0 0 8px rgba(167, 210, 33, 0.5);
  }
  .value-bullets {
    margin: 0.25rem 0 0 0.1rem;
    padding-left: 1.15rem;
    color: var(--text);
    font-size: 1rem;
    list-style: disc;
  }
  .value-bullets li {
    margin: 0.18rem 0;
    line-height: 1.6;
    opacity: 0.9;
    font-weight: 500;
  }
  .glass {
    background: linear-gradient(135deg, 
      rgba(26, 26, 26, 0.9) 0%, 
      rgba(30, 30, 30, 0.85) 100%
    );
    backdrop-filter: blur(20px);
    border-radius: 1.25rem;
    box-shadow:
      0 6px 40px rgba(0, 0, 0, 0.6),
      0 1.5px 6px rgba(167, 210, 33, 0.1);
    border: 1.5px solid rgba(167, 210, 33, 0.2);
    transition:
      box-shadow 0.18s cubic-bezier(0.22, 0.68, 0.58, 1.01),
      border 0.2s,
      transform 0.2s;
  }
  .hover-lift:hover {
    box-shadow:
      0 12px 50px rgba(167, 210, 33, 0.25),
      0 2.5px 12px rgba(0, 0, 0, 0.8);
    border-color: rgba(167, 210, 33, 0.4);
    transform: translateY(-4px) scale(1.02);
  }
  .text-green {
    color: var(--primary);
  }
  .bg-green {
    background: var(--primary);
  }
  .bg-green\/10 {
    background: rgba(167, 210, 33, 0.1);
  }
  .bg-green\/20 {
    background: rgba(167, 210, 33, 0.15);
  }
  .from-green\/20 {
    --tw-gradient-from: rgba(167, 210, 33, 0.15);
    --tw-gradient-stops: var(--tw-gradient-from),
      var(--tw-gradient-to, rgba(167, 210, 33, 0));
  }
  .to-green-light\/20 {
    --tw-gradient-to: rgba(184, 227, 46, 0.15);
  }
  .slide-up {
    animation: slideUpFadeIn 1.1s cubic-bezier(0.22, 0.68, 0.58, 1.01) 0.1s both;
  }
  @keyframes slideUpFadeIn {
    from {
      opacity: 0;
      transform: translateY(26px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }
  @media (max-width: 1024px) {
    .glass {
      padding: 1.5rem !important;
    }
    .section-divider {
      max-width: 96vw;
    }
  }
  /* Mobile responsive styles */
  @media (max-width: 480px) {
    .container {
      padding: 0 12px;
    }

    .section {
      padding: 60px 0;
    }

    .glass {
      padding: 1rem !important;
      border-radius: 16px;
    }

    .section-divider {
      margin-bottom: 1rem;
      height: 2px;
    }

    .grid.grid-cols-1.lg\\:grid-cols-2 {
      gap: 2rem;
    }

    .grid.grid-cols-1.md\\:grid-cols-2.lg\\:grid-cols-4 {
      grid-template-columns: 1fr !important;
      gap: 1.5rem;
    }

    .grid.grid-cols-1.md\\:grid-cols-2.lg\\:grid-cols-3 {
      grid-template-columns: 1fr !important;
      gap: 1.5rem;
    }

    h1 {
      font-size: 1.8rem !important;
      line-height: 1.1;
    }

    h2 {
      font-size: 1.5rem !important;
      line-height: 1.2;
      text-align: center;
    }

    h3 {
      font-size: 1.3rem !important;
    }

    h4 {
      font-size: 1.1rem !important;
    }

    .text-xl {
      font-size: 1rem !important;
    }

    .text-lg {
      font-size: 1rem !important;
    }

    .mb-16 {
      margin-bottom: 2rem;
    }

    .mb-20 {
      margin-bottom: 2.5rem;
    }

    .space-y-4 > * + * {
      margin-top: 0.75rem;
    }

    .flex.items-center.gap-4 {
      gap: 0.75rem;
      align-items: flex-start;
    }

    .dot {
      width: 0.6rem;
      height: 0.6rem;
      margin-top: 0.3rem;
      flex-shrink: 0;
    }

    .value-bullets {
      font-size: 0.9rem;
      padding-left: 1rem;
    }

    .bg-green\/10 {
      padding: 1rem;
      border-radius: 12px;
    }

    .bg-green\/10 h5 {
      font-size: 1rem;
    }

    .bg-green\/10 p {
      font-size: 0.9rem;
    }

    .hover-lift:hover {
      transform: translateY(-2px) scale(1.01);
    }

    .glass {
      padding: 1.5rem !important;
    }

    /* Improve touch targets for mobile */
    a, button {
      min-height: 44px;
      display: inline-flex;
      align-items: center;
    }
  }

  @media (max-width: 640px) {
    .grid.grid-cols-1.md\\:grid-cols-2.lg\\:grid-cols-4 {
      grid-template-columns: 1fr !important;
    }

    .grid.grid-cols-1.md\\:grid-cols-2.lg\\:grid-cols-3 {
      grid-template-columns: 1fr !important;
    }
  }

  @media (max-width: 768px) {
    .glass {
      padding: 1rem !important;
    }
    .section-divider {
      margin-bottom: 1.25rem;
    }
    .grid-cols-4,
    .grid-cols-3 {
      grid-template-columns: 1fr !important;
    }

    .grid.grid-cols-1.lg\\:grid-cols-2 {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .slide-up {
      text-align: center;
    }
  }

  /* Vision Section */
  .vision-section {
    max-width: 1000px;
    margin: 0 auto;
  }

  .vision-title {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    margin-bottom: 2rem;
  }

  .vision-content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .vision-intro,
  .vision-problem,
  .vision-solution {
    font-size: 1.125rem;
    line-height: 1.8;
    color: rgba(245, 245, 245, 0.85);
  }

  .vision-problem {
    color: rgba(245, 245, 245, 0.75);
    font-style: italic;
  }

  .vision-solution {
    color: #a7d221;
    font-weight: 600;
  }

  /* Approach Cards */
  .approach-card {
    padding: 2.5rem;
    border-radius: 20px;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .approach-icon {
    width: 64px;
    height: 64px;
    border-radius: 16px;
    background: rgba(167, 210, 33, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--primary);
    margin: 0 auto 1.5rem;
  }

  .approach-icon svg {
    width: 32px;
    height: 32px;
    stroke-width: 2;
  }

  .approach-card h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 0.75rem;
  }

  .approach-card p {
    color: rgba(245, 245, 245, 0.8);
    line-height: 1.7;
  }

  /* Leader Cards */
  .leader-card {
    padding: 2.5rem;
    border-radius: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .leader-image-wrapper {
    width: 180px;
    height: 180px;
    margin-bottom: 1.5rem;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid rgba(167, 210, 33, 0.3);
    box-shadow: 
      0 8px 24px rgba(0, 0, 0, 0.4),
      0 4px 12px rgba(167, 210, 33, 0.2);
    background: rgba(26, 26, 26, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .leader-card:hover .leader-image-wrapper {
    border-color: rgba(167, 210, 33, 0.5);
    box-shadow: 
      0 12px 32px rgba(0, 0, 0, 0.5),
      0 6px 16px rgba(167, 210, 33, 0.3);
    transform: scale(1.05);
  }

  .leader-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .leader-image-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: rgba(167, 210, 33, 0.4);
    background: rgba(26, 26, 26, 0.8);
  }

  .leader-image-placeholder svg {
    width: 80px;
    height: 80px;
  }

  .leader-name {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .leader-role {
    font-size: 1rem;
    font-weight: 600;
    color: #a7d221;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .leader-bio {
    font-size: 1.05rem;
    line-height: 1.7;
    color: rgba(245, 245, 245, 0.85);
    text-align: left;
  }

  /* Opportunity Cards */
  .opportunity-card {
    padding: 3rem 2rem;
    border-radius: 20px;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 200px;
  }

  .opportunity-value {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    color: #a7d221;
    margin-bottom: 0.75rem;
    line-height: 1;
  }

  .opportunity-label {
    font-size: 1.125rem;
    color: rgba(245, 245, 245, 0.85);
    font-weight: 600;
  }

  .opportunity-tagline {
    color: rgba(245, 245, 245, 0.9);
    font-weight: 500;
  }

  /* CTA Section */
  .cta-section {
    padding: 3rem;
    text-align: center;
    border-radius: 24px;
    max-width: 900px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: clamp(1.75rem, 3vw, 2.5rem);
    font-weight: 700;
    color: var(--text);
    margin-bottom: 1rem;
  }

  .cta-description {
    font-size: 1.15rem;
    line-height: 1.8;
    color: rgba(245, 245, 245, 0.85);
    margin-bottom: 2rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .vision-content {
      gap: 1.25rem;
    }

    .vision-intro,
    .vision-problem,
    .vision-solution {
      font-size: 1rem;
    }

    .approach-card {
      padding: 2rem;
    }

    .leader-card {
      padding: 2rem;
    }

    .leader-image-wrapper {
      width: 140px;
      height: 140px;
      margin-bottom: 1.25rem;
    }

    .leader-image-placeholder svg {
      width: 60px;
      height: 60px;
    }

    .opportunity-card {
      padding: 2rem 1.5rem;
      min-height: 160px;
    }

    .cta-section {
      padding: 2rem 1.5rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .cta-buttons .btn {
      width: 100%;
    }
  }
</style>
