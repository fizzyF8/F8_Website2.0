---
import Layout from '../layouts/Layout.astro';
import VerticalNavigation from '../components/VerticalNavigation.astro';
import UnicornStudioEmbed from '../components/UnicornStudioEmbed.jsx';
import CredibilityCarousel from '../components/CredibilityCarousel.tsx';
import VideoButton from '../components/VideoButton.jsx';
import tvm2000Modi from '../assets/kiosks/TVM 2000/tvm2000_modi.jpg';
import tvm2000Modi2 from '../assets/kiosks/TVM 2000/tvm2000_modi_2.jpg';
import tvm2000Modi3 from '../assets/kiosks/TVM 2000/tvm2000_modi_3.jpg';
import tvm2kAdVideo from '../assets/kiosks/TVM 2000/tvm2k_ad.mp4';
import tvm2kAdEnglish from '../assets/kiosks/TVM 2000/tvm2k_ad_english.mp4';
---

<Layout>
  <VerticalNavigation />
  
  <!-- Full-Page Hero Section with Unicorn Studio Background -->
  <section class="hero-section-fullpage">
    <!-- Unicorn Studio Animation Background -->
    <UnicornStudioEmbed 
      client:visible 
      projectId="1qpipNvszw8yfmoe6OyZ"
      fullPage={true}
    />
    
    <!-- Content Overlay -->
    <div class="hero-content-overlay">
      <div class="hero-content-center">
          <h1 class="hero-title">
            <span class="hero-text-line">Powering India Next:</span><br />
            <span class="hero-highlight">Seamless Journeys</span><span class="hero-text-line">, Smarter Access</span>
          </h1>
          
          <p class="hero-subtitle">
            Unattended automation. Design-first experiences. Building for the next 100 million.
          </p>
          
          <p class="hero-description">
            Frog8 builds connected platforms and intelligent kiosks that transform access - across transit, banking, and public services. From metro stations to rural banks, our solutions simplify how India moves, pays, and verifies.
          </p>
          
          <div class="hero-cta">
            <a href="/contact" class="btn btn-primary">
              <span>Schedule a Demo</span>
              <div class="btn-shine"></div>
            </a>  
            <a href="/platforms" class="btn btn-secondary">
              <span>Explore Our Offerings</span>
              <div class="btn-shine"></div>
            </a>
          </div>
      </div>
    </div>
    
    <!-- Scroll Indicator -->
    <div class="scroll-indicator">
      <div class="scroll-mouse">
        <div class="scroll-wheel"></div>
      </div>
      <p class="scroll-text">Scroll to explore</p>
    </div>
  </section>

  <!-- [Updated: Frog8 Rebuild Nov 2025 - by Fizzy] -->
  <section class="credibility-strip">
    <div class="container">
      <div class="credibility-card-redesigned glass">
        <!-- Header Section -->
        <div class="credibility-header">
          <h2 class="credibility-title">
            Trusted by India's <span class="text-primary">Leaders</span>
          </h2>
          <p class="credibility-subtitle">
            PM Modi inaugurated and used our Transigo kiosk at Namma Metro
          </p>
        </div>

        <!-- Main Content Grid -->
        <div class="credibility-content-grid">
          <!-- Left: Timeline & Info -->
          <div class="credibility-info-section">
            <!-- Milestone Event -->
            <div class="timeline-container">
              <div class="timeline-header">
                <svg class="timeline-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/>
                  <polyline points="12 6 12 12 16 14"/>
                </svg>
                <h3 class="timeline-title">A Historic Milestone</h3>
              </div>
              <div class="timeline-item">
                <div class="timeline-marker"></div>
                <div class="timeline-content">
                  <div class="timeline-date">August 10, 2025</div>
                  <div class="timeline-event">Prime Minister Narendra Modi inaugurated the BMRCL Yellow Line, a 19.15 km fully elevated metro corridor connecting Rashtreeya Vidyalaya Road to Bommasandra with 16 stations.</div>
                  <div class="timeline-location">During this historic inauguration, PM Modi personally used and inaugurated our Transigo kiosk at Namma Metro, marking a significant moment for digital transit infrastructure in India.</div>
                  <a 
                    href="https://archive.is/H8DGh" 
                    class="timeline-press-link"
                    target="_blank"
                    rel="noopener noreferrer"
                  >
                    Read Press Coverage >>
                  </a>
                </div>
              </div>
              
              <!-- Actions -->
              <div class="credibility-actions">
                <p class="credibility-video-description">Learn how to use our Transigo kiosk — tutorial by an Instagram influencer</p>
                <div class="credibility-actions-buttons">
                  <VideoButton 
                    client:load
                    videoSrc={tvm2kAdVideo}
                    englishVideoSrc={tvm2kAdEnglish}
                    className="btn btn-outline-primary"
                  >
                    Watch Tutorial
                  </VideoButton>
                </div>
              </div>
            </div>
          </div>

          <!-- Right: Image Carousel -->
          <div class="credibility-image">
            <CredibilityCarousel 
              client:load
              images={[
                { src: tvm2000Modi.src, alt: 'Prime Minister Narendra Modi inaugurating the Transigo kiosk at Namma Metro' },
                { src: tvm2000Modi2.src, alt: 'Dignitaries touring the Transigo kiosk during the Namma Metro launch' },
                { src: tvm2000Modi3.src, alt: 'Commuters engaging with the Transigo kiosk at the metro station' }
              ]}
            />
            <p class="credibility-image-description">
              <span class="neon-highlight">PM Narendra Modi</span> using our Transigo kiosk at Namma Metro, showcasing India's digital transit infrastructure.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Divider -->
  <div class="section-divider"></div>

  <!-- Our Ethos -->
  <section class="section">
    <div class="container">
      <div class="text-center mb-16">
        <h2 class="slide-up mb-6">Our <span class="text-primary">Ethos</span></h2>
        <p class="text-xl max-w-3xl mx-auto">
          We believe in building technology that bridges the gap between infrastructure and human experience.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="ethos-card" data-tilt>
          <div class="ethos-card-inner">
            <div class="ethos-icon-container">
              <div class="ethos-icon-bg"></div>
              <svg class="ethos-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <h3 class="ethos-title">Speed & Efficiency</h3>
            <p class="ethos-description">Streamlined processes that save time and reduce friction in everyday transactions.</p>
            <div class="ethos-glow"></div>
          </div>
        </div>

        <div class="ethos-card" data-tilt>
          <div class="ethos-card-inner">
            <div class="ethos-icon-container">
              <div class="ethos-icon-bg"></div>
              <svg class="ethos-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
              </svg>
            </div>
            <h3 class="ethos-title">Security First</h3>
            <p class="ethos-description">Enterprise-grade security with end-to-end encryption and compliance built-in.</p>
            <div class="ethos-glow"></div>
          </div>
        </div>

        <div class="ethos-card" data-tilt>
          <div class="ethos-card-inner">
            <div class="ethos-icon-container">
              <div class="ethos-icon-bg"></div>
              <svg class="ethos-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
              </svg>
            </div>
            <h3 class="ethos-title">Inclusive Design</h3>
            <p class="ethos-description">Solutions that work for everyone, from tech-savvy urbanites to first-time digital users.</p>
            <div class="ethos-glow"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Divider -->
  <div class="section-divider"></div>

  <!-- [Updated: Frog8 Rebuild Nov 2025 - by Fizzy] -->
  <section class="section what-we-build-section">
    <div class="container">
      <div class="text-center mb-16">
        <h2 class="slide-up mb-6">What We <span class="text-primary">Build</span></h2>
        <p class="text-xl max-w-3xl mx-auto">
          Purpose-built solutions delivering secure transit, banking, and onboarding infrastructure at scale.
        </p>
      </div>

      <div class="what-we-build-grid">
        <article class="glass solution-card">
          <div class="solution-card-header">
            <h3>Transit automation</h3>
            <p class="solution-subtitle">Transit Kiosks</p>
          </div>
          <p class="solution-description">Ticketing, Payments, Issuance</p>
          <a href="/transigo" class="btn btn-primary solution-cta">
            Explore Transigo
          </a>
        </article>

        <article class="glass solution-card">
          <div class="solution-card-header">
            <h3>Banking automation</h3>
            <p class="solution-subtitle">Self-Service Kiosks</p>
          </div>
          <p class="solution-description">Passbook Printing, CTS, Card Issuance</p>
          <a href="/navigo" class="btn btn-primary solution-cta">
            Explore Navigo
          </a>
        </article>

        <article class="glass solution-card">
          <div class="solution-card-header">
            <h3>WhatsApp workflow management</h3>
            <p class="solution-subtitle">Messaging-Native Onboarding</p>
          </div>
          <p class="solution-description">KYC, Workflow, Automation</p>
          <a href="/veriphy" class="btn btn-primary solution-cta">
            Explore Veriphy
          </a>
        </article>
      </div>
    </div>
  </section>

  <!-- Section Divider -->
  <div class="section-divider"></div>

  <!-- [Updated: Frog8 Rebuild Nov 2025 - by Fizzy] -->
  <section class="section quick-metrics-section">
    <div class="container">
      <div class="quick-metrics glass">
        <div class="metrics-item">
          <span class="metrics-value">1500+</span>
          <span class="metrics-label">Daily transactions - Transigo</span>
        </div>
        <div class="metrics-item">
          <span class="metrics-value">99.9%</span>
          <span class="metrics-label">Uptime</span>
        </div>
        <div class="metrics-item">
          <span class="metrics-value">5+</span>
          <span class="metrics-label">Active stations</span>
        </div>
        <div class="metrics-item">
          <span class="metrics-value">RBI + NCMC</span>
          <span class="metrics-label">Compliant infrastructure</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Divider -->
  <div class="section-divider"></div>

  <!-- [Updated: Frog8 Rebuild Nov 2025 - by Fizzy] -->
  <section class="section">
    <div class="container">
      <div class="glass bottom-cta">
        <h2 class="slide-up mb-6">Ready to Build Smarter Infrastructure?</h2>
        <p class="text-xl mb-8 max-w-3xl mx-auto">
          Let’s plan your rollout across transit, banking, or public service locations with resilient infrastructure.
        </p>
        <a href="/contact" class="btn btn-primary">Request a Demo</a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .p-12 { padding: 3rem; }

  /* Mobile responsive styles */
  @media (max-width: 480px) {
    .p-12 { 
      padding: 1.5rem; 
    }
    
    .hero-buttons {
      flex-direction: column;
      gap: 1rem;
    }
    
    .hero-buttons .btn {
      width: 100%;
      text-align: center;
    }
    
    .grid.grid-cols-1.md\\:grid-cols-3 {
      gap: 1.5rem;
    }
    
    .grid.grid-cols-1.md\\:grid-cols-2 {
      gap: 1.5rem;
    }
  }

  @media (max-width: 768px) {
    .p-12 { 
      padding: 2rem; 
    }
  }

  /* Full-Page Hero Section */
  .hero-section-fullpage {
    position: relative;
    width: 100vw;
    height: 100vh;
    min-height: 100vh;
    overflow: hidden;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
    margin-left: calc(-280px - 3rem);
    margin-right: calc(-100vw + 100%);
  }

  @media (max-width: 768px) {
    .hero-section-fullpage {
      width: 100% !important;
      margin-right: 0 !important;
      margin-left: 0 !important;
    }
  }

  /* Content Overlay */
  .hero-content-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 50;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(
      ellipse at center,
      rgba(10, 10, 10, 0.3) 0%,
      rgba(10, 10, 10, 0.6) 100%
    );
    padding: 2rem;
    margin: 0;
  }

  .hero-content-center {
    text-align: center;
    max-width: 1200px;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    margin: 0 auto;
    padding: 0 2rem;
    box-sizing: border-box;
  }
  
  /* Center content in full viewport (sidebar hidden in hero section) */
  @media (min-width: 1024px) {
    .hero-content-center {
      width: auto;
    }
  }

  @media (min-width: 769px) and (max-width: 1023px) {
    .hero-content-center {
      width: auto;
    }
  }

  /* Scroll Indicator */
  .scroll-indicator {
    position: absolute;
    bottom: 70px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 60;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    animation: fadeInUp 1.5s ease-out 2s both;
  }

  .scroll-mouse {
    width: 26px;
    height: 40px;
    border: 2px solid rgba(167, 210, 33, 0.6);
    border-radius: 13px;
    position: relative;
    background: rgba(10, 10, 10, 0.3);
    backdrop-filter: blur(10px);
  }

  .scroll-wheel {
    width: 3px;
    height: 8px;
    background: #a7d221;
    border-radius: 2px;
    position: absolute;
    top: 8px;
    left: 50%;
    transform: translateX(-50%);
    animation: scrollWheel 2s ease-in-out infinite;
  }

  @keyframes scrollWheel {
    0%, 100% {
      top: 8px;
      opacity: 1;
    }
    50% {
      top: 20px;
      opacity: 0.3;
    }
  }

  .scroll-text {
    color: rgba(167, 210, 33, 0.8);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateX(-50%) translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }
  }

  @keyframes fadeInUpHero {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Hero Typography */
  .hero-title {
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 700;
    line-height: 1.2;
    margin: 0 auto 1.5rem;
    color: #f5f5f5;
    text-align: center;
    width: 100%;
    text-shadow: 
      0 2px 4px rgba(0, 0, 0, 0.5),
      0 4px 8px rgba(0, 0, 0, 0.4),
      0 8px 16px rgba(0, 0, 0, 0.3);
    animation: fadeInUpHero 1s ease-out 0.3s both;
  }

  .hero-text-line {
    display: inline-block;
    color: #f5f5f5;
    text-align: center;
  }

  .hero-highlight {
    display: inline-block;
    background: linear-gradient(135deg, #a7d221, #c4e845);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: #a7d221; /* fallback color */
    position: relative;
    text-shadow: none;
    filter: drop-shadow(0 2px 8px rgba(167, 210, 33, 0.4));
  }

  /* Fallback for browsers that don't support background-clip */
  @supports not (-webkit-background-clip: text) {
    .hero-highlight {
      -webkit-text-fill-color: initial;
      color: #a7d221;
      text-shadow: 
        0 2px 4px rgba(0, 0, 0, 0.5),
        0 4px 8px rgba(167, 210, 33, 0.3);
    }
  }

  .hero-subtitle {
    color: #f5f5f5;
    font-weight: 500;
    font-size: 1.125rem;
    line-height: 1.5;
    text-align: center;
    width: 100%;
    margin: 0 auto;
    text-shadow: 
      0 2px 4px rgba(0, 0, 0, 0.6),
      0 4px 8px rgba(0, 0, 0, 0.4);
    animation: fadeInUpHero 1s ease-out 0.5s both;
  }

  .hero-description {
    color: rgba(245, 245, 245, 0.9);
    font-size: 1rem;
    line-height: 1.6;
    font-weight: 400;
    text-align: center;
    width: 100%;
    margin: 0 auto;
    text-shadow: 
      0 2px 4px rgba(0, 0, 0, 0.6),
      0 4px 8px rgba(0, 0, 0, 0.4);
    animation: fadeInUpHero 1s ease-out 0.7s both;
  }

  .hero-cta {
    animation: fadeInUpHero 1s ease-out 0.9s both;
    display: flex;
    flex-direction: row;
    gap: 1.5rem;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  .hero-cta .btn {
    min-width: 180px;
    padding: 16px 32px;
    font-size: 1rem;
    font-weight: 700;
    border-radius: 20px;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
  }

  .hero-cta .btn-primary {
    background: linear-gradient(135deg, #a7d221 0%, #b8e32e 100%);
    color: #0a0a0a;
    border: none;
  }

  .hero-cta .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(167, 210, 33, 0.4);
    background: linear-gradient(135deg, #b8e32e 0%, #c4e845 100%);
  }

  .hero-cta .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: #f5f5f5;
    border: 2px solid rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(10px);
  }

  .hero-cta .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(255, 255, 255, 0.1);
  }

  /* Enhanced Buttons */
  .btn-enhanced {
    position: relative;
    overflow: hidden;
    transform: translateY(0);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .btn-enhanced:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(167, 210, 33, 0.3);
  }

  .btn-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }

  .btn-enhanced:hover .btn-shine {
    left: 100%;
  }

  /* Credibility Strip - Redesigned */
  .credibility-strip {
    padding: 3rem 0 4rem;
    background: transparent;
  }

  .credibility-card-redesigned {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .credibility-header {
    text-align: center;
    margin-bottom: 1rem;
  }

  .credibility-title {
    font-size: clamp(1.75rem, 3vw, 2.5rem);
    font-weight: 700;
    color: #f5f5f5;
    margin-bottom: 0.75rem;
    line-height: 1.2;
  }

  .credibility-subtitle {
    font-size: 1.125rem;
    color: rgba(245, 245, 245, 0.8);
    line-height: 1.6;
    max-width: 700px;
    margin: 0 auto;
  }

  .credibility-content-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
    gap: 3rem;
    align-items: start;
  }

  .credibility-info-section {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .credibility-image {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .credibility-image-description {
    font-size: 0.9375rem;
    color: rgba(245, 245, 245, 0.8);
    line-height: 1.6;
    text-align: center;
    margin: 0;
    padding: 0 0.5rem;
  }

  .neon-highlight {
    color: #a7d221;
    font-weight: 700;
    text-shadow: 
      0 0 10px rgba(167, 210, 33, 0.6),
      0 0 20px rgba(167, 210, 33, 0.4),
      0 0 30px rgba(184, 227, 46, 0.2);
    letter-spacing: 0.02em;
  }

  /* Timeline Styles */
  .timeline-container {
    background: rgba(18, 18, 18, 0.6);
    border-radius: 16px;
    padding: 1.5rem;
    border: 1px solid rgba(167, 210, 33, 0.15);
  }

  .timeline-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
  }

  .timeline-icon {
    width: 24px;
    height: 24px;
    color: #a7d221;
  }

  .timeline-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: #f5f5f5;
    margin: 0;
  }

  .timeline-item {
    position: relative;
    padding-left: 2rem;
  }

  .timeline-marker {
    position: absolute;
    left: 0;
    top: 0.5rem;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: #a7d221;
    border: 3px solid rgba(167, 210, 33, 0.2);
    box-shadow: 0 0 0 4px rgba(167, 210, 33, 0.1);
  }

  .timeline-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .timeline-date {
    font-size: 1rem;
    font-weight: 700;
    color: #a7d221;
  }

  .timeline-event {
    font-size: 1rem;
    font-weight: 500;
    color: #f5f5f5;
    line-height: 1.6;
    margin-bottom: 0.75rem;
  }

  .timeline-location {
    font-size: 0.9375rem;
    color: rgba(245, 245, 245, 0.7);
    line-height: 1.6;
    margin-bottom: 0.75rem;
  }

  .timeline-press-link {
    display: inline-block;
    font-size: 0.9375rem;
    font-weight: 600;
    color: #a7d221;
    text-decoration: none;
    margin-top: 0.5rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    text-shadow: 0 0 10px rgba(167, 210, 33, 0.5);
    letter-spacing: 0.02em;
  }

  .timeline-press-link:hover {
    color: #b8e32e;
    text-shadow: 
      0 0 15px rgba(167, 210, 33, 0.8),
      0 0 25px rgba(184, 227, 46, 0.4);
    transform: translateX(4px);
  }

  .timeline-press-link:active {
    transform: translateX(2px);
  }


  .credibility-actions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(167, 210, 33, 0.15);
  }

  .credibility-video-description {
    font-size: 0.875rem;
    color: rgba(245, 245, 245, 0.6);
    text-align: center;
    margin: 0 0 0.5rem 0;
    line-height: 1.5;
  }

  .credibility-actions-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
  }

  .btn.btn-outline-primary {
    background: transparent;
    color: #a7d221;
    border: 2px solid rgba(167, 210, 33, 0.7);
    box-shadow: none;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .btn.btn-outline-primary:hover {
    background: linear-gradient(135deg, rgba(167, 210, 33, 0.15), rgba(184, 227, 46, 0.25));
    color: #0a0a0a;
    border-color: rgba(167, 210, 33, 0.9);
    box-shadow: 0 12px 24px rgba(167, 210, 33, 0.2);
    transform: translateY(-2px);
  }

  .btn.btn-outline-primary:active {
    transform: translateY(0);
  }

  /* What We Build */
  .what-we-build-section {
    background: transparent;
  }

  .what-we-build-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.75rem;
  }

  .solution-card {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    position: relative;
    overflow: hidden;
    align-items: center;
    text-align: center;
  }

  .solution-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(167, 210, 33, 0.08), rgba(184, 227, 46, 0.04));
    opacity: 0;
    transition: opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
  }

  .solution-card:hover::after {
    opacity: 1;
  }

  .solution-card-header {
    text-align: center;
  }

  .solution-card-header h3 {
    font-size: 1.75rem;
    font-weight: 700;
    color: #f5f5f5;
    margin-bottom: 0.25rem;
  }

  .solution-subtitle {
    font-size: 1rem;
    color: rgba(245, 245, 245, 0.7);
    font-weight: 500;
  }

  .solution-description {
    color: rgba(245, 245, 245, 0.75);
    font-size: 1rem;
    text-align: center;
  }

  .solution-cta {
    align-self: center;
  }

  /* Quick Metrics */
  .quick-metrics-section {
    background: transparent;
  }

  .quick-metrics {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.5rem;
    padding: 2rem 2.5rem;
    align-items: stretch;
  }

  .metrics-item {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    align-items: center;
    text-align: center;
  }

  .metrics-value {
    font-size: 1.75rem;
    font-weight: 700;
    color: #a7d221;
  }

  .metrics-label {
    font-size: 0.95rem;
    color: rgba(245, 245, 245, 0.75);
  }

  /* Bottom CTA */
  .bottom-cta {
    padding: 3rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }

  /* Ethos Cards */
  .ethos-card {
    perspective: 1000px;
    height: 320px;
  }

  .ethos-card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, 
      rgba(26, 26, 26, 0.9) 0%, 
      rgba(30, 30, 30, 0.85) 100%
    );
    backdrop-filter: blur(20px);
    border-radius: 20px;
    padding: 2rem;
    border: 1px solid rgba(167, 210, 33, 0.2);
    transition: all 0.6s cubic-bezier(0.23, 1, 0.320, 1);
    transform-style: preserve-3d;
    overflow: hidden;
    box-shadow: 
      0 8px 32px rgba(0, 0, 0, 0.4),
      0 4px 16px rgba(167, 210, 33, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
  }

  .ethos-card:hover .ethos-card-inner {
    transform: rotateY(5deg) rotateX(5deg) translateZ(20px);
    border-color: rgba(167, 210, 33, 0.4);
    box-shadow: 
      0 20px 60px rgba(167, 210, 33, 0.25),
      0 10px 30px rgba(0, 0, 0, 0.8),
      0 4px 16px rgba(167, 210, 33, 0.12),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
  }

  .ethos-icon-container {
    position: relative;
    width: 80px;
    height: 80px;
    margin: 0 auto 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ethos-icon-bg {
    position: absolute;
    inset: 0;
    background: conic-gradient(from 0deg, #a7d221, #bdfc52, #a7d221);
    border-radius: 50%;
    opacity: 0.2;
    animation: iconRotate 8s linear infinite;
  }

  .ethos-icon-bg::before {
    content: '';
    position: absolute;
    inset: 3px;
    background: linear-gradient(135deg, 
      rgba(26, 26, 26, 0.95) 0%, 
      rgba(30, 30, 30, 0.9) 100%
    );
    border-radius: 50%;
    backdrop-filter: blur(10px);
  }

  @keyframes iconRotate {
    to { transform: rotate(360deg); }
  }

  .ethos-icon {
    width: 40px;
    height: 40px;
    color: #a7d221;
    z-index: 2;
    transition: all 0.3s ease;
  }

  .ethos-card:hover .ethos-icon {
    color: #b8e32e;
    transform: scale(1.1) rotate(5deg);
  }

  .ethos-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: #a7d221;
    margin-bottom: 1rem;
    text-align: center;
    transition: all 0.3s ease;
  }

  .ethos-card:hover .ethos-title {
    color: #b8e32e;
    transform: translateY(-2px);
  }

  .ethos-description {
    color: rgba(245, 245, 245, 0.8);
    text-align: center;
    line-height: 1.6;
    transition: all 0.3s ease;
  }

  .ethos-card:hover .ethos-description {
    color: rgba(245, 245, 245, 0.95);
  }

  .ethos-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(167, 210, 33, 0.1) 0%, transparent 70%);
    transform: translate(-50%, -50%);
    opacity: 0;
    transition: opacity 0.6s ease;
    pointer-events: none;
  }

  .ethos-card:hover .ethos-glow {
    opacity: 1;
  }

  /* Staggered Animation */
  .ethos-card:nth-child(1) { animation: cardSlideIn 0.8s ease-out 0.2s both; }
  .ethos-card:nth-child(2) { animation: cardSlideIn 0.8s ease-out 0.4s both; }
  .ethos-card:nth-child(3) { animation: cardSlideIn 0.8s ease-out 0.6s both; }

  @keyframes cardSlideIn {
    from {
      opacity: 0;
      transform: translateY(60px) scale(0.9);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  /* Mobile Responsiveness */
  @media (max-width: 768px) {
    .hero-section-fullpage {
      min-height: 100vh;
      height: auto;
      width: 100% !important;
      margin-left: 0 !important;
      margin-right: 0 !important;
    }

    .hero-content-overlay {
      position: absolute !important;
      top: 0 !important;
      left: 0 !important;
      right: 0 !important;
      bottom: 0 !important;
      width: 100% !important;
      height: 100% !important;
      padding: 100px 1.5rem 1.5rem;
      z-index: 50;
      display: flex !important;
      align-items: center;
      justify-content: center;
    }

    .hero-content-center {
      width: 100%;
      max-width: 100%;
      padding: 0 1rem;
      gap: 1.25rem;
      text-align: center;
    }

    .hero-title {
      font-size: clamp(1.75rem, 6vw, 2.5rem);
      margin-left: 0 !important;
      width: 100%;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .hero-description {
      font-size: 0.9375rem;
      line-height: 1.5;
    }

    .hero-cta {
      flex-direction: column;
      gap: 1rem;
      width: 100%;
      max-width: 400px;
      margin: 0 auto;
    }

    .hero-cta .btn {
      width: 100%;
      min-width: auto;
      padding: 14px 28px;
    }

    .scroll-indicator {
      bottom: 40px;
    }

    .scroll-text {
      font-size: 0.75rem;
    }

    .ethos-card {
      height: 280px;
    }

    .ethos-card-inner {
      padding: 1.5rem;
    }

    .ethos-card:hover .ethos-card-inner {
      transform: translateY(-5px);
      border-color: rgba(167, 210, 33, 0.4);
      box-shadow: 
        0 12px 40px rgba(167, 210, 33, 0.2),
        0 6px 20px rgba(0, 0, 0, 0.6),
        inset 0 1px 0 rgba(255, 255, 255, 0.08);
    }

    .ethos-icon-container {
      width: 60px;
      height: 60px;
    }

    .ethos-icon {
      width: 30px;
      height: 30px;
    }

    .credibility-card-redesigned {
      padding: 1.75rem;
      gap: 2rem;
    }

    .credibility-content-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .credibility-image {
      order: -1;
      gap: 1rem;
    }

    .credibility-image-description {
      font-size: 0.875rem;
      padding: 0;
    }

    .credibility-info-section {
      order: 1;
      gap: 1.5rem;
    }

    .credibility-header {
      text-align: left;
    }

    .credibility-subtitle {
      text-align: left;
    }

    .timeline-container {
      padding: 1.25rem;
    }

    .credibility-actions {
      width: 100%;
      align-items: center;
      margin-top: 1.5rem;
      padding-top: 1.25rem;
    }

    .credibility-actions-buttons {
      width: 100%;
      flex-direction: column;
      gap: 0.75rem;
    }

    .credibility-actions-buttons .btn {
      width: 100%;
    }

    .credibility-video-description {
      font-size: 0.8125rem;
    }

    .what-we-build-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .solution-card {
      padding: 2rem;
    }

    .quick-metrics {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      padding: 1.75rem;
    }

    .metrics-item {
      align-items: center;
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .hero-content-overlay {
      position: absolute !important;
      top: 0 !important;
      left: 0 !important;
      right: 0 !important;
      bottom: 0 !important;
      width: 100% !important;
      height: 100% !important;
      padding: 90px 1rem 1rem;
    }

    .hero-content-center {
      gap: 1.5rem;
    }

    .hero-title {
      font-size: 1.5rem;
      line-height: 1.2;
    }

    .hero-subtitle {
      font-size: 0.9375rem;
    }

    .hero-description {
      font-size: 0.875rem;
      line-height: 1.4;
    }

    .hero-cta {
      max-width: 320px;
      margin: 0 auto;
    }

    .hero-cta .btn {
      padding: 12px 24px;
      font-size: 0.9375rem;
    }

    .scroll-indicator {
      display: none;
    }

    .credibility-card-redesigned {
      padding: 1.5rem;
      gap: 1.75rem;
    }

    .credibility-title {
      font-size: 1.5rem;
    }

    .credibility-subtitle {
      font-size: 1rem;
    }

    .credibility-image-description {
      font-size: 0.8125rem;
    }

    .timeline-container {
      padding: 1rem;
    }

    .timeline-title {
      font-size: 1rem;
    }

    .credibility-actions {
      flex-direction: column;
      align-items: stretch;
      gap: 0.75rem;
      margin-top: 1.5rem;
      padding-top: 1.25rem;
    }

    .credibility-actions-buttons {
      width: 100%;
      flex-direction: column;
      gap: 0.75rem;
    }

    .credibility-actions-buttons .btn {
      width: 100%;
    }

    .credibility-video-description {
      font-size: 0.75rem;
    }

    .what-we-build-grid {
      grid-template-columns: 1fr;
    }

    .quick-metrics {
      grid-template-columns: 1fr;
      gap: 1.25rem;
      padding: 1.5rem;
    }

    .bottom-cta {
      padding: 2.25rem 1.5rem;
    }
  }

  @media (max-width: 1024px) {
    .hero-section-fullpage {
      margin-left: calc(-260px - 2rem);
    }

    .hero-content-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      padding: 2rem;
    }
  }

  @media (min-width: 1920px) {
    .hero-content-center {
      max-width: 1600px;
    }

    .hero-title {
      font-size: 5.5rem;
    }

    .hero-subtitle {
      font-size: 1.5rem;
    }

    .hero-description {
      font-size: 1.25rem;
    }
  }
</style>